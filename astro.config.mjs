// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://adminralh.github.io',
  base: '/astro_test', // WICHTIG: Der Name deines Repositories
});